package com.acme.keeplo.platform.wishlist.domain.model.queries;
import com.acme.keeplo.platform.wishlist.domain.model.valueobjects.CollectionId;

public record GetCollectionByIdQuery(CollectionId id) {
}
